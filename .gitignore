# Node
node_modules/
server/dist/
packages/*/dist/
autocad/server/dist/
*.tgz

# .NET
bin/
obj/
*.user
*.suo
*.vs/
*.DotSettings

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*
!.env.example
!.env.*.example
.npmrc

# Private keys and signing material
*.key
*.pem
*.p12
*.pfx

# Build artifacts
*.nupkg
updater/publish/
/.artifacts/

# Claude Code session state (local-only)
.claude/

# Local agent context may contain project/client details. Public guidance is
# maintained in CLAUDE.md and CONTRIBUTING.md.
/AGENTS.md

# Local project/customer-specific working files (never publish)
/.local/

# Ad-hoc working files / scratch exports
# (Data dumps, DAX/M expression drafts, generated TMDL — not source)
scratch/
*.pbix
*.pbip
*.pbit
/1st_floor_beams.json
/beam_*.txt
/expr_*.txt
/powerbi/

# Daily work files (organized into 작업자료/YYYY-MM-DD/ — local only)
작업자료/

# Office app lock files (auto-deleted when app closes)
.~lock.*#
~$*

# Spreadsheets / data files at repo root (local working data)
*.xlsx
*.xls
*.xlsm

# Side-by-side clones / legacy copies (have their own .git)
gstack/
revit-mcp-v2/
